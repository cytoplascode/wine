{
  "name": "label-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Offline PWA that turns a wine label photo into an Obsidian Wine note",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8000",
    "eval": "node eval/run.mjs"
  },
  "devDependencies": {
    "@huggingface/transformers": "^3.8.1"
  }
}
